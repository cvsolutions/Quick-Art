<% title = 'Art Directory | Scambio Links | Awards | Artisti' %>
<%- layout('layout') %>

<div class="row">
    <div class="col-xs-8">
        <h1>Art Directory Scambio Links</h1>
        <p>Se vuoi scambiare un link con il nostro Portale, inserisci un link reciproco usando le seguenti informazioni e riempi il form sottostante. Appena il tuo link sarà verificato, verrai aggiunto all’elenco nei successivi 7-10 giorni.</p>
        <p>&nbsp;</p>

        <div class="well">
            <div class="row">
                <% directories.forEach( function (row){ %>
                <div class="col-xs-6">
                    <div class="media">
                        <a class="pull-left" target="_blank" href="<%= row.web %>">
                            <img src="/images/info.png" class="media-object js-rc-64" alt="<%= row.fullname %>"/>
                        </a>
                        <div class="media-body">
                            <h4 class="media-heading"><%= row.fullname %></h4>
                            <p><%= row.category.fullname %></p>
                        </div>
                    </div>
                    <br/>
                </div>
                <% }); %>
            </div>
        </div>

        <hr/>
        <div id="result"></div>
        <form id="js-directory-form" method="post" enctype="application/x-www-form-urlencoded">
            <div class="row">
                <div class="col-xs-6">
                    <div class="form-group">
                        <label><span class="text-danger">*</span> Titolo</label>
                        <input type="text" class="form-control" name="fullname" id="fullname" value="" required="">
                    </div>
                </div>
                <div class="col-xs-6">
                    <div class="form-group">
                        <label><span class="text-danger">*</span> Categoria</label>
                        <select class="form-control" name="category" id="category" required="">
                            <option value="">-</option>
                            <% categories.forEach( function (row){ %>
                            <option value="<%= row._id %>"><%= row.fullname %></option>
                            <% }); %>
                        </select>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <label><span class="text-danger">*</span> Breve descrizione</label>
                <textarea class="form-control" name="description" id="description" rows="10" required=""></textarea>
            </div>
            <div class="form-group">
                <label><span class="text-danger">*</span> Sito Internet</label>
                <input type="url" class="form-control" name="web" id="web" value="" required="">
            </div>
            <p>* I siti non riguardanti l’Arte non saranno inclusi.</p>
            <input type="submit" class="btn btn-default" value="Invia Richiesta">
        </form>
    </div>
    <div class="col-xs-4">
        <div class="list-group">
            <% categories.forEach( function (row){ %>
            <a href="/directory/<%= row.slug %>" class="list-group-item">Categoria <%= row.fullname %></a>
            <% }); %>
        </div>
        <div class="panel panel-default">
            <div class="panel-body">
                <%- include adsbygoogle-300-250.ejs %>
            </div>
        </div>
    </div>
</div>
